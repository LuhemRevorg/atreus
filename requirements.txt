# Direct dependencies only -- let the resolver pick compatible transitive versions.
# whispermlx constrains torch/torchaudio/transformers/tokenizers, so those are
# deliberately not pinned here.

# speech-to-text
whispermlx

# wake word
openwakeword
ai-edge-litert          # provides the tflite_runtime shim in src/wake/tflite_compat.py
PyAudio

# llm
ollama
claude-agent-sdk

# macOS UI (AppKit / Foundation / PyObjCTools)
pyobjc-framework-Cocoa

# misc
numpy
pillow
requests
python-dotenv
